<%= form_with(model: shelf) do |form| %>
  <% if shelf.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(shelf.errors.count, "error") %> prohibited this shelf from being saved:</h2>

      <ul>
        <% shelf.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    Select Shop to add shelf to<br>
  <%= form.select :shop_id, Shop.all.collect{|s| [s.name, s.id]}, include_blank: true,
    id: :shelf_shop_id, prompt: "Shop Name" %>
    <br></br>
  </div>

  <div>
    <%= form.submit :Submit, class: 'btn btn-primary' %>
  </div>
<% end %>
